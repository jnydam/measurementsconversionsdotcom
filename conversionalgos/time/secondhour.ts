// Second -- Hour Conversions

export const convertSecondsToHours = (seconds: number) => {
  return seconds / 3600;
};

export const convertHoursToSeconds = (hours: number) => {
  return hours * 3600;
};
