// Second -- Minute Conversions

export const convertSecondsToMinutes = (seconds: number) => {
  return seconds / 60;
};

export const convertMinutesToSeconds = (minutes: number) => {
  return minutes * 60;
};
